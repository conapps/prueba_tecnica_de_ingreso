---
- name: Incidente de laboratorio
  hosts: cisco
  gather_facts: false
  tasks:
    - name: Aplicar cambio en equipo
      cisco.ios.ios_config:
        lines:
          - shutdown
        parents: "interface {{ fail_interface }}"
      when: inventory_hostname == fail_host
      no_log: true
